# Laravel App | API Auth
authorization app with laravel api jwt token


## Installation

You can install the package via composer:

``` bash
composer install
```


then run server:

``` bash
php artisan serv
```
